# 本地开发说明（无 HTTPS、无域名）

本地环境**不使用 HTTPS 和域名**，直接使用 `http://127.0.0.1:8002` 即可。

## 前端报错说明

若出现：

```text
POST http://127.0.0.1:8002/v1/chat/completions net::ERR_CONNECTION_REFUSED
[execWebChat] error: Failed to fetch
```

表示**本机 8002 端口没有服务在监听**，需要先启动 Chat Router 服务。

## 本地快速启动 Chat Router

### 方式一：只启动 Chat Router（推荐本地开发）

```bash
# 在项目根目录执行
./start_chat_router_local.sh
```

或手动启动：

```bash
cd aio_server
source ../export_env_local.sh   # 加载环境变量（如 Gateway 地址、Token）
python3 chat_router_server.py   # 前台运行，Ctrl+C 停止
```

### 方式二：启动全部 AIO-Pod 服务

```bash
./start_aio_pod.sh
```

会同时启动：File Server (8001)、Exec Server (8000)、Chat Router (8002)。

## 本地 API 地址（无需 HTTPS/域名）

| 用途       | 本地地址 |
|------------|----------|
| Chat 对话  | `http://127.0.0.1:8002/v1/chat/completions` |
| 模型列表   | `http://127.0.0.1:8002/v1/models` |
| 健康检查   | `http://127.0.0.1:8002/health` |

前端配置示例：

- **Base URL**: `http://127.0.0.1:8002` 或 `http://127.0.0.1:8002/v1`
- **协议**: HTTP（不要用 HTTPS）
- **域名**: 不需要，用 IP + 端口即可

## 依赖条件

1. **Python 环境需包含 uvicorn**  
   Chat Router 依赖 `uvicorn`、`fastapi` 等。若直接运行报 `ModuleNotFoundError: No module named 'uvicorn'`：
   ```bash
   conda activate aiopod
   pip install -r aio_server/requirements.txt
   ```
   然后重新执行 `./start_aio_pod.sh` 或 `./start_chat_router_local.sh`。  
   `start_aio_pod.sh` 会自动激活 aiopod 并安装依赖；`start_chat_router_local.sh` 会优先使用 aiopod 环境中的 Python。

2. **OpenClaw Gateway 已运行**  
   Chat Router 会连到 Gateway（默认 `127.0.0.1:18789`）。若未启动 Gateway，健康检查会显示 `gateway: disconnected`，聊天会失败。

3. **环境变量**  
   确保 `export_env_local.sh` 里至少配置了：
   - `OPENCLAW_GATEWAY_HOST`
   - `OPENCLAW_GATEWAY_PORT`
   - `OPENCLAW_GATEWAY_TOKEN`

## 验证本地服务

```bash
# 1. 检查端口是否有进程
lsof -i :8002

# 2. 健康检查
curl http://127.0.0.1:8002/health

# 3. 测试对话接口
curl -X POST http://127.0.0.1:8002/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"openclaw:main","messages":[{"role":"user","content":"你好"}]}'
```

## 本地 vs 生产

| 项目     | 本地开发           | 生产环境（云端）     |
|----------|--------------------|----------------------|
| 协议     | HTTP               | HTTPS                |
| 地址     | `http://127.0.0.1:8002` | `https://webchat.aio2030.fun` |
| 域名     | 不需要             | 需要                 |
| 启动方式 | `./start_chat_router_local.sh` 或 `./start_aio_pod.sh` | Nginx + 后端服务 |

生产环境才需要 Nginx、域名和 HTTPS；本地只要把 Chat Router 跑起来并用 `http://127.0.0.1:8002` 即可。
